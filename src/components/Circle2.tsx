import React from 'react'
import { Circle } from 'react-shapes';
import { isAbsolute } from 'path';

function Circle2(){
    
    return <Circle r={100} fill={{color: '#FFFFFF'}} stroke={{color:'#3DE1DC '}} strokeWidth={25} />
}

export default Circle2